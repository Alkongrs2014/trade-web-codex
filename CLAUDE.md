# webtrade — مرصد الأسهم الأمريكية

تطبيق ويب عربي (RTL) لأكبر 70 شركة أمريكية: أسعار، شارت شموع، مؤشرات فنية،
أساسيات، مزاج السوق، أخبار، حاسبة مخاطرة، تنبيهات. بلا أي مكتبة خارجية.

**المرجع الكامل: `SPEC.md`** — اقرأه قبل أي تغيير معماري.

---

## ⚠️ أهم شيء: مشروع واحد في مكانين

هذا المشروع يعيش في موضعين، وهما **نسختان من نفس المستودع لا مشروعان**:

| الموضع | الدور |
|---|---|
| `D:\Ai\ClaudeCode\trade\webtrade` (محلي) | التطوير والتشغيل الفعلي |
| `github.com/Alkongrs2014/Alkongrs2014` | مصدر الحقيقة + نشر الصفحة |

**قواعد ملزمة لمنع التباعد:**

1. **الفرع الافتراضي `main` هو مصدر الحقيقة.** أي تغيير يُدمج فيه.
2. **ابدأ كل جلسة بـ `git pull`** وانتهِ بـ `git push`. لا تترك عملاً
   غير مدفوع — الموضع الآخر لن يراه.
3. **لا تعدّل نفس الملف في الموضعين دون مزامنة بينهما.** لو شككت،
   `git status` و`git log --oneline -5` أولاً.
4. **مجلد `data/` و`.env` محليان فقط** — مستبعدان في `.gitignore`
   عمداً. `data/` بيانات متولّدة، و`.env` يحوي مفاتيح.
5. فرع `data` في GitHub منفصل تماماً عن `main` (orphan) — تكتبه مهام
   GitHub Actions. **لا تدمجه في `main` أبداً.**

### الفرق الوحيد المقصود بين الموضعين
متغيّر `PREFER_YAHOO`:
- محلياً `=1` → Yahoo أولاً (غير محظور، بلا سقف، توازٍ)
- على GitHub غير مضبوط → Twelve Data أولاً بميزانية محدودة

نفس الشيفرة، سلوك مختلف حسب الشبكة. **لا تفرّع الشيفرة لنسختين.**

---

## البنية

```
stocks/index.html      التطبيق كاملاً (5 تبويبات: الآن، الأسهم، الفرص، تحليل، الأخبار)
stocks/config.js       الإعدادات — يكتشف مكانه تلقائياً (localhost أم ويب)
stocks/symbols.json    كون الرموز بثلاث طبقات (انظر «الطبقتان» أدناه)
scripts/build-universe.mjs أداة صيانة تبني symbols.json وتتحقق من كل رمز
scripts/fetch-quotes.mjs   أسعار فقط — الحلقة السريعة (كل دقيقتين)
scripts/fetch-market.mjs   أسعار + شموع + مؤشرات (كل 10 دقائق)
scripts/fetch-news.mjs     أخبار عربية ومترجمة (مع كل تحديث سوق)
scripts/fetch-daily.mjs    أساسيات + ترتيب (يومي)
scripts/lib/news.mjs       خلاصات RSS + الترجمة وذاكرتها
scripts/lib/session.mjs    حالة جلسة السوق — مشتركة بين المهمتين
scripts/lib/finnhub.mjs    أسعار لحظية وأساسيات
scripts/lib/twelvedata.mjs شموع (أساسي سحابياً)
scripts/lib/yahoo.mjs      شموع (أساسي محلياً) + Stooq احتياطاً
scripts/lib/indicators.mjs المؤشرات — مشتركة بين الخادم والمتصفح
scripts/lib/options.mjs    Black–Scholes والجريكس واستخراج التقلّب الضمني
stocks/scans.js            شروط الماسح — نسخة واحدة للمتصفح والخادم
scripts/backtest.mjs       الأرشيف التاريخي — سجلّ كل شرط على خمس سنوات
scripts/track-signals.mjs  السجلّ الحيّ — يثبّت ما ظهر فعلاً ويتابعه
scripts/fetch-events.mjs   الأحداث القوية — تقويم الفدرالي الرسمي
scripts/fetch-options.mjs  عقود الخيارات — دورة نصف ساعة مستقلة
local/run.mjs          المشغّل المحلي (env + جلب + خادم + نشر)
local/*.bat            اختصارات ويندوز
```

---

## الأوامر

```bash
# الفحص الذاتي لكل مهمة — بلا شبكة، شغّلها كلها قبل أي دفع
node scripts/fetch-quotes.mjs --check
node scripts/fetch-market.mjs --check
node scripts/fetch-news.mjs   --check
node scripts/fetch-daily.mjs  --check
node scripts/fetch-options.mjs --check
node scripts/backtest.mjs      --check
node scripts/track-signals.mjs --check
node scripts/fetch-events.mjs  --check
node scripts/build-universe.mjs --check

node local/run.mjs quotes                # أسعار فقط (~90 ثانية)
node local/run.mjs market                # شموع ومؤشرات + أخبار
node local/run.mjs news                  # أخبار وحدها
node local/run.mjs options               # عقود الخيارات (دورة نصف ساعة)
node local/run.mjs backtest              # الأرشيف التاريخي (يومياً، ~500 طلب)
node local/run.mjs signals               # تثبيت إشارات اليوم (بلا شبكة)
node local/run.mjs events                # تقويم الفدرالي (يومياً)
node local/run.mjs both                  # كل شيء
node local/run.mjs serve                 # خادم على localhost:8080
node local/run.mjs publish               # نشر data/ على فرع data
node local/run.mjs quotes --publish      # جلب ثم نشر (النشر بشرط نجاح الجلب)
```

---

## معرفة تشغيلية مكتسبة بالتجربة

> هذه ليست آراء — كل بند كلّف تشخيصاً فعلياً. لا تعِد اكتشافها.

### مصادر البيانات — الواقع لا التوثيق
| المصدر | الحقيقة المُختبَرة |
|---|---|
| Yahoo | ⛔ يرفض عناوين السحابة بـ429 · ✅ يعمل منزلياً |
| Finnhub مجاني | أسعار وأساسيات ✅ · توزيع التوصيات وصفقات المطّلعين ✅ · **الشموع والتقويم الاقتصادي 403** · رموز `^GSPC` مرفوضة |
| Twelve Data مجاني | شموع ✅ · **8/دقيقة و800/يوم** |
| Stooq | ⛔ يعيد صفحة حظر **بحالة 200** لا 429 |
| تقويم الفدرالي | ✅ `federalreserve.gov/json/calendar.json` بلا مفتاح ولا حصّة |
| مكتب إحصاءات العمل (BLS) | ✅ **بترويسات متصفح كاملة** · 403 بترويسة ناقصة — لا يحجب الآلات بل الطلب الذي لا يشبه المتصفح |
| خيارات ياهو | ✅ بالـcrumb · **يصفّر bid/ask/IV خارج الجلسة** (انظر أدناه) |

### فرضيات جُرّبت وثبت خطؤها — لا تكرّرها
1. ❌ «التراجع الطويل يحل 429» — يعلّق التشغيل 20 دقيقة بلا فائدة.
   الحظر ممتد لا عابر؛ **الفشل السريع أفضل** لأنه يُبقي آخر بيانات سليمة.
2. ❌ «ترويستا Referer/Origin تقللان الرفض» — **العكس**. إرسال `Origin`
   من الخادم يجعل الطلب يبدو تجاوزاً لـ CORS.
3. ❌ «بصمة TLS هي السبب» — `curl` نجح مرة ثم فشل؛ كان حظ عنوان IP.

### الطبقتان — لماذا لا كون واحد
| الطبقة | العدد | ما يُجلب | الدورة |
|---|---|---|---|
| الأساسية `symbols` | 90 مرشّحاً، يُختار أعلى 70 | 15د + ساعة + يومي، أساسيات، أخبار | 10 دقائق |
| `crypto` | 26 عملة | نفس الفريمات الثلاثة | 10 دقائق |
| `wide` | 414 (بقية S&P 500) | **اليومي وحده** | يومياً، بسقف `WIDE_PER_RUN` لكل تشغيل |

جلب 510 رموز × 3 فريمات كل عشر دقائق = 1500 طلب/دورة، وهو ما يستدعي 429
حتى من شبكة منزلية. اليوميُّ وحده يكفي للبحث ولمستويات الدعم والمقاومة
و52 أسبوعاً — وهو كل ما يُطلب من رمز خارج المرصودة.

- **`wide.json` تراكمي** — كل تشغيل يجدّد حصّته ويدمجها فوق القديم. بوابة
  سلامة ترفض كتابته أقصر مما كان.
- **`u` في صفوف `wide.json` عمر الشمعات لا عمر السعر.** `fetch-quotes`
  يحدّث السعر ولا يمسّ `u` — لمسه يجعل `fetch-market` يظنّ اليوميّ حديثاً
  فلا يجدّده أبداً.
- **الكريبتو خارج إحصاء السوق** (الاتساع، المزاج، القطاعات، الرابحون
  والخاسرون). مداه اليومي أوسع بمراتب، فبيتكوين وحده يزيح المتوسط ويحتل
  القوائم كل يوم. وفترات التداول تُؤخذ من رمز أمريكي حصراً، وإلا قالت
  الترويسة «السوق مفتوح» ليل السبت.
- **الأسماء العربية للأساسية والكريبتو فقط.** الطبقة الواسعة تعرض اسمها
  الإنجليزي: الترجمة الآلية (MyMemory) تشوّه أسماء الشركات، والإنجليزي
  الصحيح أصدق من عربي مفبرك. `nm(r)` في الواجهة تتكفّل بالسقوط.
- **البحث يعمّ الكون كاملاً بمجرّد كتابة نص** حتى والمُبدّل على «المرصودة»
  — من كتب «بيتكوين» يقصده، وحصر البحث افتراضياً يعيد «لا نتائج» عن رمز
  موجود.

### ثلاث دورات لا دورة واحدة
| المهمة | الدورة | لماذا |
|---|---|---|
| `fetch-quotes` | دقيقتان | السعر وحده. 70 رمزاً × طلب = ~90 ثانية عند حد 60/دقيقة |
| `fetch-market` | 10 دقائق | الشمعة المكتملة لا تتغيّر كل دقيقة، وإعادة جلبها تزاحم الأسعار على نفس الحصّة |
| `fetch-daily` | يومياً | الأساسيات تتغيّر مرة كل ربع سنة |

**لا تحسب المؤشرات في `fetch-quotes`.** تحديث السعر بلا تحديث الشمعة لا
يغيّر RSI ولا MACD فعلاً، وحسابها من سعر واحد يعطي رقماً كاذباً.

### مصائد كامنة أُصلحت — احذر إعادتها
- **الشمعات تُحفظ مضغوطة** `[t,o,h,l,c,v]` وتحتاج `unpackCandles` عند
  القراءة. بدونها: `Cannot read properties of undefined (reading 'toFixed')`.
- **صلاحية فريم 15د صفر** = «قديم دائماً». لذا تُرتَّب المعالجة حسب
  الحاجة لا القيمة السوقية، وإلا احتكرت الرموز الممتلئة الميزانية.
- **عدّاد قاطع الدورة لـ Yahoo وحده** — Stooq يعيد 200 فيصفّره لو شملته.
- **لا تبتلع الأخطاء** — `catch {}` في حلقة على 70 رمزاً يخفي السبب.
  اطبع أول خطأ فعلي وعدده.
- **اعرض `—` لا رقماً ملفّقاً** عند غياب أي حقل. الشمعة الناقصة تُتخطّى.
- **`isFinite` العالمية تحوّل `null` إلى صفر** فتقبله كرقم صالح. استعمل
  `Number.isFinite`. سهم بلا سعر كان يدخل متوسط قطاعه كـ«تغيّر 0%».
- **`normPct` لِعائد التوزيعات وحده.** ROE ونمو الإيراد والهامش كسور
  تتجاوز الواحد بشكل مشروع (ROE إنفيديا 1.17 = 117%)، فتُضرب في 100
  دائماً عبر `asPct`. `normPct` كانت تعيدها كما هي فتُعرض «1%».
- **`market.json` لا يحفظ `proxy` إلا حين يُستعمل**، فمصدر بدائل
  المؤشرات هو `symbols.json` لا الملف المحفوظ — وإلا تجمّدت المؤشرات.
- **فترات التداول المحفوظة تصف يوم جلبها.** استعمالها في اليوم التالي
  يعطي «بعد الإغلاق» والسوق مفتوح. `statusNow` ترجع للتقدير حينها.

- **ياهو يصفّر `bid` و`ask` و`impliedVolatility` للخيارات خارج ساعات
  التداول** ولا يُبقي إلا `lastPrice` و`openInterest` و`lastTradeDate`.
  الفلتر الذي يشترط عرضاً وطلباً يعيد **صفر عقد** ليلاً — وهو ما حدث في
  أول تشغيل. لذلك وضعان: `live` بعرض وطلب حقيقيين، و`last` يستخرج
  التقلّب الضمني من سعر آخر صفقة بالتنصيف. الوضع يُقرأ من نسبة العقود
  المسعَّرة لا من ساعة الحائط، ولا يكفي عقدٌ أو عقدان مسعَّران لقلب
  السلسلة إلى `live` — كان ذلك يُفرغ سلاسل PANW وCRM تماماً.
- **في الوضع المغلق يُقاس الزمن من لحظة السعر لا من الآن.** سعرُ عقدٍ
  نُفِّذ الجمعة مع زمنٍ متبقٍّ محسوب الثلاثاء يخلط لحظتين، فيُنسب فارق
  الزمن كلّه إلى التقلّب: تقلّب VZ الضمني خرج 117% بدل 22%.
- **`divY` في `fundamentals.json` مخزَّن بمقياسين**: أبل `0.0034` (كسر)
  وفيرايزون `5.5842` (نسبة مئوية) — هكذا يعطيه ياهو. الواجهة تتعايش عبر
  `normPct` لأنها تعرض النسبة، أما الحساب فلا: تمرير `5.5842` كعائد
  توزيعات إلى Black–Scholes يخفض السعر الآجل إلى 59% من الفوري. أي
  مستهلك حسابي يجب أن يطبّع بنفسه (`divYield` في `fetch-options`).

- **تواريخ ياهو للعملات الرقمية تحمل أسعاراً "ما قبل الإدراج"**: UNI-USD
  يقفز من 0.000038 إلى 0.598 في يوم — عائد 1,573,986%. رقم واحد كهذا وسط
  نصف مليون ملاحظة رفع متوسط عائد اليوم الواحد ثلاث نقاط مئوية، فصار خط
  أساس الأرشيف 19% لعشرين يوماً بدل 1%. لذلك: الكريبتو خارج الأرشيف،
  و`trimArtifacts` تقطع السلسلة عند أي قفزة يومية فوق 80%، والإحصاء
  **بالوسيط لا المتوسط**.
- **ملفات `sym/*.json` تحفظ 260 شمعة يومية فقط** (`KEEP`)، وهي أقصر من
  فترة تسخين الأرشيف نفسها (EMA200 + نافذة 52 أسبوعاً = 260). فالأرشيف
  يجلب تاريخه بنفسه في الذاكرة ولا يخزّنه — خمس سنوات لخمسمئة رمز
  ~25 ميغابايت تُنشر بلا أن يقرأها أحد، ومخرَجه ثلاثة كيلوبايتات.

- **تقريب الأسعار بالخانات العشرية يمحو الأصول الرخيصة.** شيبا إينو
  بسعر `0.0000051` صار صفراً مع `Math.round(v*10000)/10000` — وكذلك كل
  شمعاته وATR وشرارته. `rp()` تقرّب بالأرقام المعنوية لما دون الواحد
  وبأربع خانات لما فوقه، وهي **مكرّرة عمداً في `fetch-market` و
  `fetch-quotes`** لأن اختلافها بينهما يجعل السعر يقفز بين الدورتين.
  وفي الواجهة `priceDigits` تفعل الشيء نفسه للعرض.
  لم يظهر هذا إلا حين رفضت بوابة النشر الصفَّ — وهي تعمل لهذا بالضبط.

### مصائد ويندوز في سكربتات التشغيل — كلّفت تشخيصاً فعلياً
- **`cmd.exe` يفسد ملف `.bat` بـUTF-8 تحت `chcp 65001`.** يتتبّع موضعه
  بالبايتات ويعيد قراءته بعد كل أمر، فمع نص عربي طويل يختلّ التتبّع
  وتُقرأ الأوامر مقطّعة (`'/SC' is not recognized`). ولا يفشل بوضوح:
  بعض المهام تُنشأ وبعضها لا. لذلك منطق الجدولة كله في `schedule.ps1`،
  وملفات `.bat` أسطر إنجليزية قليلة تستدعيه.
- **`.ps1` بلا BOM تقرأها PowerShell 5.1 كـANSI** فيتشوّه العربي وينهار
  التحليل. تُحفَظ UTF-8 **مع BOM**، و`.gitattributes` يثبّت ذلك.
- **`Register-ScheduledTask` ترفض التكرار اللانهائي**: `[TimeSpan]::MaxValue`
  يخرج منها `P99999999DT23H59M59S` فيردّ المجدول «قيمة خارج المدى».
  الإنشاء بـ`schtasks.exe /SC MINUTE /MO n`، والضبط بعده بـ`Set-ScheduledTask`.
- **`WshShell.Run` بلا انتظار يعطّل حمايات المجدول الثلاث.** كان
  `run-hidden.vbs` يعود فور إطلاق node، فيسجّل المجدول «نجح 0x0» قبل أن
  يبدأ العمل: السقف الزمني لا يقتل تشغيلاً عالقاً، ونتيجة آخر تشغيل تقول
  «نجح» دائماً، و`IgnoreNew` لا يرى تداخلاً قط. صار ينتظر ويعيد رمز
  الخروج الحقيقي. قفل `data/.run.lock` وُجد أصلاً لأن حماية المجدول
  كانت معطّلة بصمت.

- **حجم عقد الخيارات مقابل مراكزه القائمة يتضخّم قرب الانتهاء بطبيعته**
  (المراكز تُغلق قبل الاستحقاق)، فتصدّرت قائمةَ «النشاط غير المعتاد»
  عقودُ الغد بنسب 25–50 ضعفاً — تحيّز بنيوي لا إشارة. `FLOW.minDays`
  يستبعد ما يبقى له أقل من أسبوع.
- **`options.json` تراكمي مثل `wide.json`**: الطبقة الواسعة تُجدَّد
  بالتدوير (`OPT_WIDE_PER_RUN`)، فبلا دمج يخرج الملف بحصّة التشغيل
  الأخير وحدها وتختفي بقية الرموز من الواجهة. وصفٌّ قديم يبقى بقيمه حتى
  يأتي دوره — وهو سلوك مقصود لا خلل.

### حالة قائمة (لم تُحلّ)
**جدولة GitHub Actions لم تشتغل ولا مرة** — صفر تشغيل من نوع `schedule`
رغم أن المهمة نشطة والمستودع عام وغير مشتقّ، وجُرّبت إزاحة الدقائق.
التشغيل المحلي عبر Task Scheduler يتجاوز هذا كلياً.

> تحديث 2026-09-03: فرع `data` صار يتلقّى دفعات من المهمة فعلاً (آخرها
> 19:45 UTC)، فالجدولة تعمل الآن على الأقل بشكل متقطّع. لم أستطع تمييز
> `schedule` من `workflow_dispatch` بلا `gh` على هذا الجهاز.

### بيانات السحابة ناقصة لا معطّلة — والنشر المحلي هو الحل
قياس 2026-09-03 على فرع `data`: **56 رمزاً من 70 قديم**، وشمعات NVDA
عمرها ستة أيام، بينما السعر سعر اليوم. السبب معروف: Finnhub يعطي السعر
من السحابة، وYahoo محظور، وميزانية Twelve Data تكفي 14 رمزاً لكل تشغيل.

النتيجة أسوأ من غياب البيانات لأنها لا تبدو خطأً — شارت ومؤشرات ومستويات
تُحسب من شمعات قديمة تحت سعر حديث.

`node local/run.mjs publish` يدفع `data/` المحلي إلى فرع `data` بنفس
طريقة المهمة (فرع يتيم بدفعة `-f`) وبنفس بوابة السلامة. مهمة GitHub
تسحب فرع `data` قبل كل تشغيل، فما تنشره من جهازك يبقى ولا يُداس.

---

## قواعد العمل

- **الفحص الذاتي أولاً:** `--check` يمرّ بلا شبكة في ثانية. شغّله قبل أي دفع.
- **تحقّق من البيانات لا من «نجح التشغيل»:** ثلاثة أخطاء حقيقية اختبأت
  خلف تشغيلات ناجحة. افحص `data/summary.json` و`data/meta.json` فعلياً.
- **بوابة السلامة مقدّسة:** لا تُكتب بيانات فوق بيانات سليمة عند الفشل
  التام (`if (!rows.length) throw`). لا تُضعفها لتمرير تشغيل.
- **`indicators.mjs` مشترك** بين الخادم والمتصفح — لا تكرّر منطقه بنسختين
  وإلا اختلفت الأرقام بين القائمة والشارت.
- **التعليقات تشرح السبب لا الفعل** — الشيفرة تقول ماذا، والتعليق لماذا.
  الملفات كلها بالعربية؛ حافظ على ذلك.

---

## المفاتيح

محلياً في `.env` (انظر `.env.example`)، وعلى GitHub في
`Settings → Secrets and variables → Actions`:

| المفتاح | الأثر عند غيابه |
|---|---|
| `FINNHUB_API_KEY` | لا أسعار لحظية (تُشتق من إغلاق آخر شمعة) |
| `TWELVEDATA_API_KEY` | لا شموع سحابياً · **غير مطلوب محلياً** |

**لا ترفع `.env` إطلاقاً.** ولا تضع أي مفتاح في ملف يصل المتصفح.
