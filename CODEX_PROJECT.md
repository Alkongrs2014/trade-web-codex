# CODEX_PROJECT.md — دليل سريع للمشروع

> نسخة مستقلة مطوّرة من مرصد الأسهم الأمريكية، موجودة محلياً في
> `D:\Ai\Gpt\trade-web-codex`. منذ 2026-09-13 أصبحت منتج **مرصد PRO**:
> رادار فرص تفسيري، مختبر قرار، قوة نسبية، محفظة محلية، مقاييس عقود
> متقدمة، وطبقة تخزين للعمل دون انقطاع.
>
> مستودع هذه النسخة هو `Alkongrs2014/trade-web-codex`. لا تعدّل أو
> تنشر إلى `Alkongrs2014/Alkongrs2014` من داخلها.
>
> المرجع المعماري الكامل: **`SPEC.md`**. المعرفة التشغيلية المكتسبة
> بالتجربة (مصائد المزوّدين، فرضيات ثبت خطؤها، مصائد ويندوز): **`CLAUDE.md`**.
> اقرأهما قبل أي تغيير معماري — كل بند فيهما كلّف تشخيصاً فعلياً.

---

## 1. Architecture

تطبيق ويب عربي (RTL) لمتابعة أكبر ~70 شركة أمريكية + 26 عملة رقمية +
بقية S&P 500: أسعار، شارت شموع، مؤشرات فنية، أساسيات، مزاج السوق، أخبار،
خيارات، ماسح فرص، حاسبة مخاطرة، ومتابعة تغيّر الفرص بين الزيارات.

```
┌─────────────────┐   يكتب    ┌──────────┐   يقرأ   ┌──────────────────┐
│ scripts/*.mjs   │ ────────► │  data/   │ ───────► │ stocks/index.html│
│ (Node، جلب)     │  ملفات    │  *.json  │  fetch   │ (الموقع الثابت)   │
└─────────────────┘   JSON    └──────────┘          └──────────────────┘
        │                          ▲
        │ APIs خارجية               │ محلياً: node local/run.mjs
        ▼                          │ سحابياً: GitHub Actions → فرع data
  Finnhub · TwelveData · Yahoo · RSS · federalreserve.gov · bls.gov
```

**قاعدتان تحكمان كل شيء:**
1. **بلا أي مكتبة خارجية** — لا في المتصفح ولا في Node. لا React، لا
   Chart.js، لا axios. الشارت مرسوم يدوياً على `<canvas>`، والطلبات
   بـ `fetch` العارية. لا تُدخِل اعتمادية جديدة.
2. **الشيفرة المشتركة نسخة واحدة** — `scripts/lib/indicators.mjs`
   و`stocks/scans.js` يقرأهما الخادم والمتصفح كلاهما. تكرار منطقهما
   بنسختين يجعل الأرقام تختلف بين القائمة والشارت.

---

## 2. Backend أم static؟

**لا يوجد backend. المشروع static بالكامل.**

- الموقع صفحة HTML واحدة تُقدَّم كملف ثابت (GitHub Pages في الأصل).
- «الخادم» في `local/run.mjs serve` مجرّد خادم ملفات ثابتة للتطوير.
- `worker/worker.js` هو Cloudflare Worker **اختياري** للسعر اللحظي،
  **وغير مفعّل** — `LIVE_ENDPOINT` في `stocks/config.js` فارغ، فالموقع
  يعمل بالكامل من ملفات `data/`.
- لا قاعدة بيانات. التخزين ملفات JSON (انظر §9).

---

## 3. Install / Dev / Build

| الأمر | الحقيقة |
|---|---|
| **install** | **لا شيء.** لا `package.json`، لا `node_modules`، لا اعتماديات. يكفي Node 18+ (لأجل `fetch` المدمجة). |
| **dev** | `node local/run.mjs serve` → http://localhost:8080 (يغيّره `PORT=8081`) |
| **build** | **لا شيء.** لا خطوة بناء ولا ترجمة ولا تجميع. الملفات تُقدَّم كما هي. |
| **test** | `node scripts/<name>.mjs --check` — فحص ذاتي بلا شبكة لكل سكربت، يمرّ في ثانية |

```bash
# الفحص الذاتي الكامل (بلا شبكة) — شغّله قبل أي دفع
node scripts/fetch-quotes.mjs  --check
node scripts/fetch-market.mjs  --check
node scripts/fetch-news.mjs    --check
node scripts/fetch-daily.mjs   --check
node scripts/fetch-options.mjs --check
node scripts/backtest.mjs      --check
node scripts/track-signals.mjs --check
node scripts/fetch-events.mjs  --check
node scripts/build-universe.mjs --check
node scripts/test-evaluate.mjs
```

```bash
# جلب البيانات (يحتاج شبكة ومفاتيح .env)
node local/run.mjs quotes      # أسعار فقط (~90 ثانية)
node local/run.mjs market      # شموع + مؤشرات + أخبار
node local/run.mjs options     # عقود الخيارات
node local/run.mjs both        # كل شيء
node local/run.mjs serve       # خادم العرض
```

> **تحذير:** `data/` محلي وغير متتبّع على `main`. قد تحتوي نسخة ويندوز
> على لقطة من بيانات الأصل، لكن النشر يكون إلى فرع `data` في المستودع
> الجديد فقط. الجلب يستهلك حصص المزوّدين المجانية، فلا تشغّله إلا عند الحاجة.

---

## 4. الملفات الرئيسية

| الملف | الدور |
|---|---|
| `stocks/index.html` | التطبيق الأساسي وسبعة تبويبات: الآن، الأسهم، الفرص، PRO، التحليل، السجل، الأخبار |
| `stocks/pro.js` · `stocks/pro.css` | مختبر PRO: دقة، محفزات، تغيّر الزيارة، دليل الأداء، خيارات، ومحفظة |
| `stocks/sw.js` | تخزين غلاف التطبيق وآخر ملفات بيانات سليمة |
| `stocks/config.js` | الإعدادات — يكتشف مكانه تلقائياً (localhost أم ويب) |
| `stocks/scans.js` | شروط ماسح الفرص — **نسخة واحدة يقرأها المتصفح والخادم** |
| `stocks/plan.js` | **نواة خطة الصفقة** — المستويات والاتجاه والهدف والوقف. نسخة واحدة يقرأها المتصفح والخادم |
| `stocks/evaluate.js` | **طبقة التقييم** — الطزاجة وجودة الدخول والزمن المتوقّع والآفاق. **لا تغيّر التوصية** |
| `scripts/learn.mjs` | **طبقة التحليل** — إحصاء أداء الإشارات. **تقرأ ولا تكتب في المحرّك** |
| `scripts/analytics.mjs` | قوة نسبية وBeta وارتباط وفجوات من الشمعات الموجودة بلا حصص API |
| `scripts/intelligence.mjs` | مكتب دقة يرفض الفرص غير المكتملة ويجمع الفني والمالي والخبر والدليل الزمني |
| `scripts/lib/intelligence.mjs` | رتب مالية قطاعية وبوابات رفض قابلة للاختبار |
| `scripts/check-ui.mjs` | بوابة سلامة للواجهة والتبويبات والمعرّفات والنحو |
| `stocks/symbols.json` | كون الرموز بثلاث طبقات (§8) |
| `scripts/lib/indicators.mjs` | المؤشرات الفنية — **مشتركة بين الخادم والمتصفح** |
| `scripts/lib/options.mjs` | Black–Scholes والجريكس والتقلّب الضمني |
| `local/run.mjs` | المشغّل المحلي (env + جلب + خادم + نشر) |

`stocks/index.html` مقسّم بعناوين `/* ===== */`. أهمها بالترتيب:

```
 845  1) إعدادات ومساعدات        2154  تسلسل التوصية («منذ متى»)
 969  تشخيص (DIAG)               2313  سجل الصفقات
1015  2) حسابات المؤشرات         2608  خطة الصفقة (Target/Stop)
1113  3) حالة السوق              2635  الفحص الذاتي لنواة الخطة
1158  4) جلب البيانات            2824  المستويات المتوقّعة وتتبّعها
1231  5) العرض — الشريط العلوي   2973  drawChart() — رسم الشارت
1437  الأحداث القوية             3098  التحليل الأساسي
1465  «وش أشتري الآن؟»           3277  ماسح الفرص
1603  أرشيف النتائج              3412  6) حاسبة المخاطرة
1707  ملخّص عقود الخيارات        3506  7) التنقّل والتحميل الكسول
1758  عقود الخيارات (CALL/PUT)   3553  8) التنقّل
2054  قراءة السهم بالكلام        3630  9) التشغيل والتحديث
```

> أرقام الأسطر صحيحة وقت أخذ اللقطة. لو انزاحت، ابحث بعنوان القسم العربي.

---

## 5. أين يقع كل منطق

### Recommendation Engine — «وش أشتري الآن؟»
- **`stocks/index.html:1465`** — ترشيح بمعايير يضبطها المستخدم ثم ترتيب.
- المرحلتان منفصلتان عمداً: **ترشيح** (استبعاد ما لا يحقّق الشروط) ثم
  **ترتيب** (وزن المرشّحين بخطة الصفقة). المعايير في نموذج التبويب
  (`pkRR`، `pkEarn` … حول `index.html:638`).
- يعتمد على `scans.js` للشروط، وعلى خطة الصفقة (§Target/Stop) للترتيب.

### CALL / PUT logic
- **المحرّك الرياضي: `scripts/lib/options.mjs`**
  | الدالة | الدور |
  |---|---|
  | `bs({S,K,T,r,q,sigma,type})` | Black–Scholes — `type: "call" \| "put"` |
  | `impliedVol(price, {...})` | استخراج التقلّب الضمني بالتنصيف |
  | `liquid(c, f, mode, now)` | فلتر السيولة — وضعان `live` و`last` |
  | `chainMode(contracts)` | يقرّر الوضع من نسبة العقود المسعَّرة لا من الساعة |
  | `flowRatio(c)` / `flowLabel(vx)` | النشاط غير المعتاد (حجم ÷ مراكز قائمة) |
  | `evaluate(c, {...})` | تقييم عقد واحد كاملاً |
  | `rank(evaluated, band, limit)` | ترتيب واختيار أفضل العقود |
- **الجلب: `scripts/fetch-options.mjs`** (دورة نصف ساعة مستقلة) →
  `data/options.json` (تراكمي).
- **العرض: `stocks/index.html:1758`** (بطاقات العقود، كول أخضر وبوت أحمر)
  و`:1707` (ملخّص السوق: أين الخيارات غالية وأين رخيصة).
- ⚠️ **مصيدتان موثّقتان في `CLAUDE.md`** لا تُعِد اكتشافهما: ياهو يصفّر
  `bid`/`ask`/`IV` خارج الجلسة (ومن هنا وضعا `live`/`last`)، و`divY` في
  `fundamentals.json` مخزَّن بمقياسين مختلفين فيجب تطبيعه قبل تمريره
  إلى Black–Scholes.

### Target / Stop calculations
- **النواة: `stocks/plan.js`** — **هنا الرياضيات، وهي المصدر الوحيد**:
  | الدالة | الدور |
  |---|---|
  | `levelsFrom({k4h,k1d,px,a,w52h,w52l})` | استخراج الدعوم والمقاومات |
  | `planDirOf(score)` | اتجاه الخطة من النتيجة الفنية (شراء/بيع) |
  | `planFrom({px,atr,resAll,supAll}, d)` | الدخول والهدف والوقف حسب الاتجاه |
  | `validatePlan(p)` | التحقّق من تماسك الخطة |
  **لماذا ملف مستقل:** السجلّ (`history.json`) يحفظ الخطة **كما رآها
  المستخدم** لحظة الإشارة، والخادم هو من يكتبه. لو عاشت الرياضيات في
  `index.html` وحدها لاحتاج الخادم نسخة ثانية — ونسختان من نفس الحساب
  تتباعدان. **لا تكرّر منطق `plan.js` في مكان آخر.**
- **المحوِّل في الواجهة: `stocks/index.html:2608`** (`tradePlan`) — مجرّد
  غلاف فوق `planFrom`، لا حساب موازٍ.
- **فحص ذاتي للنواة: `stocks/index.html:2635`** — يعمل بـ `?selftest=1`.
- **المستويات المتوقّعة وتتبّعها: `stocks/index.html:2824`**.
- **حاسبة المخاطرة اليدوية: `stocks/index.html:3412`** + الحقول
  `iCap` / `iRisk` / `iEntry` / `iStop` / `iTarget`.
- **الاتجاه مهم:** الخطة موقّعة باتجاه — الهدف والوقف ينعكسان بين الشراء
  والبيع. كان خطأً سابقاً يقيس الهبوط صعوداً (أُصلح في `7d3010f`).

### طبقتان فوق التوصية — لا داخلها
قاعدة صريحة في كلا الملفين: **لا تغيّران التوصية ولا الخطة ولا أي نتيجة
فنية**؛ تقرآن ما حسبه المحرّك وتجيبان أسئلة أخرى.
- **`stocks/evaluate.js`** — `freshness` (عمر الإشارة)، `entryQuality`
  (هل ما زال الدخول جيداً)، `etaFor` (الزمن المتوقّع للهدف)،
  `horizonsOf` (الآفاق الزمنية).
- **`scripts/learn.mjs`** — `statsOf` / `breakdown` / `weekly` /
  `findingsOf`: إحصاء أداء الإشارات تاريخياً → `data/learn.json`،
  والعرض في `index.html:2542` (`renderLearn`).
- **سجل الصفقات: `index.html:2313`** — يحفظ ما قيل لحظة الإشارة.

### Opportunities / Scanner
- **الشروط: `stocks/scans.js`** — نسخة واحدة يقرأها المتصفح والخادم.
  هنا تُضاف أو تُعدَّل شروط الماسح، ولا مكان آخر.
- **الواجهة: `stocks/index.html:3277`**.
- **الأرشيف التاريخي: `scripts/backtest.mjs`** → `data/backtest.json`
  (سجلّ كل شرط على خمس سنوات) وعرضه في `index.html:1603`.
- **السجلّ الحيّ: `scripts/track-signals.mjs`** → `data/signals.json`
  (يثبّت ما ظهر فعلاً ويتابعه).

### Analysis
- **فني:** `scripts/lib/indicators.mjs` (خادم) ونسخته المرآة في
  `index.html:1015` — RSI، MACD، EMA، ATR، الدعم/المقاومة، النتيجة الكلية.
- **أساسي:** `scripts/fetch-daily.mjs` → `data/fundamentals.json`،
  والعرض في `index.html:3098`.
- **مزاج السوق والاتساع والقطاعات:** `index.html:1113` + `:1231`.
- **قراءة بالكلام:** `index.html:2054` — يلخّص الفني والمالي والأخبار.
- **الأحداث الكلية:** `scripts/fetch-events.mjs` → `data/events.json`،
  العرض في `index.html:1437`.

### Charts
- **`stocks/index.html:2973` → `drawChart()`** — رسم يدوي على
  `<canvas id="chart">` (`:401`). لا مكتبة شارت.

### Timeframes
- **`stocks/index.html` (حول `:855`، ابحث عن `const TFS`)**:
  ```js
  const TFS       = ["15m", "1h", "4h", "1d"];
  const TF_WEIGHT = { "15m": 0.5, "1h": 1, "4h": 1.5, "1d": 2 };
  ```
  النتيجة الكلية متوسط موزون على الفريمات الأربعة — اليومي أثقل.
- **يُجلب ثلاثة فقط** (15m، 1h، 1d). **4h مشتقّ من 1h على الخادم**
  بالتجميع: `scripts/fetch-market.mjs:196` (`aggregate(c, 4)`, `derived: true`).
- الطبقة الواسعة (§8) لها **اليومي وحده**.
- دورات التحديث ثلاث لا واحدة:

| المهمة | الدورة | لماذا |
|---|---|---|
| `fetch-quotes` | دقيقتان | السعر وحده — **لا تُحسب المؤشرات هنا**، حسابها من سعر واحد يعطي رقماً كاذباً |
| `fetch-market` | 10 دقائق | الشمعة المكتملة لا تتغيّر كل دقيقة |
| `fetch-daily` | يومياً | الأساسيات تتغيّر مرة كل ربع سنة |
| `fetch-options` | نصف ساعة | مستقلة |

---

## 6. APIs

| المصدر | الملف | ما يقدّمه | الواقع المُختبَر |
|---|---|---|---|
| Finnhub | `scripts/lib/finnhub.mjs` | أسعار لحظية، أساسيات، توصيات، صفقات المطّلعين | مجاني ✅ · **الشموع والتقويم 403** · `^GSPC` مرفوض |
| Twelve Data | `scripts/lib/twelvedata.mjs` | شموع (الأساسي سحابياً) | **8/دقيقة و800/يوم** |
| Yahoo | `scripts/lib/yahoo.mjs` | شموع (الأساسي محلياً) + خيارات + Stooq احتياطاً | ⛔ يرفض عناوين السحابة بـ429 · ✅ يعمل منزلياً |
| RSS + ترجمة | `scripts/lib/news.mjs` | أخبار عربية ومترجمة + ذاكرة ترجمة | — |
| SEC + Federal Reserve RSS | `scripts/lib/news.mjs` | مصادر أولية رسمية تُرفع فوق المقالات | ✅ بلا مفتاح |
| federalreserve.gov | `scripts/fetch-events.mjs` | تقويم الفدرالي | ✅ بلا مفتاح ولا حصّة |
| bls.gov | `scripts/fetch-events.mjs` | التضخّم وتقرير الوظائف | ✅ **بترويسات متصفح كاملة** — 403 بترويسة ناقصة |

**المفاتيح:** ملف `.env` في الجذر (انظر `.env.example`). **غير مرفوع إلى
Git إطلاقاً** — مستبعد في `.gitignore`.

| المفتاح | الأثر عند غيابه |
|---|---|
| `FINNHUB_API_KEY` | لا أسعار لحظية (تُشتق من إغلاق آخر شمعة) |
| `TWELVEDATA_API_KEY` | لا شموع سحابياً · **غير مطلوب محلياً** |
| `PREFER_YAHOO=1` | محلياً: Yahoo أولاً (أسرع وبلا سقف) |

**لا تضع أي مفتاح في ملف يصل المتصفح** (`stocks/*`).

---

## 7. تخزين البيانات

ملفات JSON في `data/` — لا قاعدة بيانات، ولا API وقت التشغيل.

| الملف | المحتوى |
|---|---|
| `summary.json` | لقطة موجزة لكل الرموز (السعر والتغيّر والنتيجة) — أول ما يُقرأ |
| `market.json` | حالة السوق: الاتساع، القطاعات، المؤشرات، الرابحون والخاسرون |
| `sym/<SYM>.json` | شمعات الرمز ومؤشراته — **260 شمعة يومية فقط** (`KEEP`) |
| `wide.json` | الطبقة الواسعة (اليومي فقط) — **تراكمي** |
| `options.json` | سلاسل العقود — **تراكمي** |
| `fundamentals.json` | الأساسيات (ربع سنوية) |
| `news.json` · `events.json` | الأخبار · التقويم الاقتصادي |
| `backtest.json` · `signals.json` · `archive.json` | الأرشيف التاريخي · السجلّ الحيّ · أرشيف النتائج |
| `learn.json` · `history.json` | إحصاء أداء الإشارات (`learn.mjs`) · سجل الصفقات كما رآها المستخدم |
| `intelligence.json` | فرص الدقة، المراقبة، الرتب المالية، غرفة الأخبار، ومحفزات الاقتصاد والأرباح |
| `ranking.json` · `meta.json` · `i18n.json` | الترتيب · بيانات التشغيل · الترجمات |

**تفاصيل تكسر الشيفرة إن جُهلت:**
- **الشمعات مضغوطة** `[t,o,h,l,c,v]` وتحتاج `unpackCandles` عند القراءة.
  بدونها: `Cannot read properties of undefined (reading 'toFixed')`.
- **`wide.json` و`options.json` تراكميان** — كل تشغيل يجدّد حصّته ويدمجها
  فوق القديم. الكتابة بحصّة التشغيل وحدها تُفقد بقية الرموز.
- **`u` في صفوف `wide.json` عمر الشمعات لا عمر السعر.**
- **بوابة سلامة** ترفض الكتابة فوق بيانات سليمة عند الفشل التام
  (`if (!rows.length) throw`) وترفض ملفاً أقصر مما كان. **لا تُضعِفها.**

**من أين يقرأ المتصفح** — `stocks/config.js:14`:
```js
DATA_BASE: (["localhost","127.0.0.1",""].includes(location.hostname))
  ? "../data"                                    // محلياً
  : "https://raw.githubusercontent.com/Alkongrs2014/trade-web-codex/data"
```
هذا الفصل مقصود: نسخة Codex لا تقرأ فرع بيانات المشروع الأصلي.

---

## 8. الطبقات الثلاث للرموز (`stocks/symbols.json`)

| الطبقة | العدد | ما يُجلب | الدورة |
|---|---|---|---|
| `symbols` (الأساسية) | 90 مرشّحاً → أعلى 70 | 15د + ساعة + يومي + أساسيات + أخبار | 10 دقائق |
| `crypto` | 26 | نفس الفريمات الثلاثة | 10 دقائق |
| `wide` | 414 (بقية S&P 500) | **اليومي وحده** | يومياً بسقف `WIDE_PER_RUN` |

**لماذا لا كون واحد:** 510 رموز × 3 فريمات كل عشر دقائق = 1500 طلب/دورة،
وهو ما يستدعي 429 حتى من شبكة منزلية.

**قواعد مقصودة لا أخطاء:**
- **الكريبتو خارج إحصاء السوق** (الاتساع، المزاج، القطاعات، القوائم) —
  مداه اليومي أوسع بمراتب فيزيح المتوسط. وفترات التداول تُؤخذ من رمز
  أمريكي حصراً.
- **الأسماء العربية للأساسية والكريبتو فقط** — الطبقة الواسعة تعرض اسمها
  الإنجليزي (الترجمة الآلية تشوّه أسماء الشركات).
- **البحث يعمّ الكون كاملاً** بمجرّد كتابة نص، حتى والمُبدّل على «المرصودة».

---

## 9. تشغيل تلقائي (اختياري)

`local/schedule.ps1` يسجّل مهام Windows Task Scheduler.
**لم يُفعَّل في هذه النسخة عمداً** — تفعيله يجعل نسختين تتزاحمان على حصص
المزوّدين نفسها. لا تشغّله إلا بعد إيقاف جدولة الأصل.

`.github/workflows/` منسوخة كما هي للتطابق، وستفشل في مستودع جديد بلا
أسرار. أوقف Actions من إعدادات المستودع إن أزعجت.

---

## 10. قواعد العمل في هذا المشروع

- **الفحص الذاتي أولاً:** `--check` يمرّ بلا شبكة في ثانية. قبل أي دفع.
- **تحقّق من البيانات لا من «نجح التشغيل»** — افحص `data/summary.json`
  و`data/meta.json` فعلياً.
- **بوابة السلامة مقدّسة** — لا تُضعَف لتمرير تشغيل.
- **اعرض `—` لا رقماً ملفّقاً** عند غياب أي حقل.
- **استعمل `Number.isFinite` لا `isFinite` العالمية** (الأخيرة تحوّل
  `null` إلى صفر فتقبله كرقم صالح).
- **التعليقات تشرح السبب لا الفعل.** الملفات كلها بالعربية — حافظ على ذلك.
