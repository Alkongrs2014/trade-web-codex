# مرصد الأسهم الأمريكية — مواصفات كاملة لإعادة البناء

وثيقة مكتفية بذاتها لبناء المشروع من الصفر في أي مكان. تصف المعمارية،
مصادر البيانات وحدودها الحقيقية، صِيَغ الملفات، ومنطق الحسابات — إضافة
إلى **العقبات التي اصطدم بها المشروع فعلياً وحلولها**، وهي الجزء الذي
يوفّر عليك أياماً لو بنيته من جديد.

---

## 1) ما هو المشروع

تطبيق ويب عربي (RTL) يعرض أكبر ٧٠ شركة أمريكية بالقيمة السوقية: أسعار،
شارت شموع، مؤشرات فنية، أساسيات الشركة، مزاج السوق، أخبار، حاسبة مخاطرة،
وتنبيهات سعرية.

**قيد التصميم الجوهري:** لا يوجد مصدر مجاني للأسهم الأمريكية يسمح للمتصفح
بالاتصال به مباشرة (بعكس Binance للعملات الرقمية) — كلها تمنع CORS أو
تتطلب مفتاحاً لا يجوز كشفه في المتصفح.

**الحل المعتمد:** مهمة مجدولة تجلب البيانات من طرف الخادم وتكتبها ملفات
JSON ثابتة على فرع Git منفصل، والصفحة الثابتة تقرأ منها عبر
`raw.githubusercontent.com` (يدعم CORS). النتيجة: بلا خادم، بلا تكلفة،
وبلا كشف أي مفتاح.

```
GitHub Actions (كل 10 دقائق)          فرع data (ملفات JSON)         المتصفح
  Finnhub  → أسعار لحظية      ──┐
  TwelveData → شموع            ──┼──→  summary.json               fetch()
  RSS → أخبار                  ──┤     market.json        ────→   صفحة ثابتة
  (Yahoo → محاولة ثانية)        ──┘     sym/<SYMBOL>.json          (GitHub Pages)
                                       fundamentals.json
                                       ranking.json / news.json / meta.json
```

---

## 2) بنية الملفات

```
.nojekyll                  تعطيل معالجة Jekyll (الموقع HTML ثابت)
stocks/
  index.html               التطبيق كاملاً — بلا أي مكتبة خارجية (~2400 سطر)
  config.js                الإعدادات القابلة للتعديل وحدها
  scans.js                 شروط ماسح الفرص — نسخة واحدة للمتصفح والخادم
  symbols.json             ثلاث طبقات: 90 مرشّحاً أساسياً (يُختار أعلى 70)،
                           و414 في الطبقة الواسعة، و26 عملة رقمية
scripts/
  fetch-quotes.mjs         الحلقة السريعة: أسعار فقط (كل دقيقتين)
  fetch-market.mjs         الدورية: شموع + مؤشرات + مزاج السوق (كل 10 دقائق)
  fetch-options.mjs        عقود الخيارات والجريكس (كل 30 دقيقة)
  fetch-news.mjs           أخبار عربية ومترجمة
  fetch-daily.mjs          اليومية: ترتيب + أساسيات + توصيات + مطّلعون
  fetch-events.mjs         تقويم الفدرالي — أحداث قوية قادمة
  backtest.mjs             الأرشيف التاريخي: سجلّ كل شرط على خمس سنوات
  track-signals.mjs        السجلّ الحيّ: يثبّت ما ظهر فعلاً ويتابعه
  build-universe.mjs       أداة صيانة تبني symbols.json وتتحقق من كل رمز
  lib/
    finnhub.mjs            أسعار وأساسيات + توزيع التوصيات وصفقات المطّلعين
    twelvedata.mjs         شموع (المصدر الأساسي سحابياً)
    yahoo.mjs              شموع وأسعار وسلاسل الخيارات + Stooq احتياطياً
    indicators.mjs         حسابات المؤشرات (مشتركة بين الخادم والمتصفح)
    options.mjs            Black–Scholes والجريكس واستخراج التقلّب الضمني
    session.mjs            حالة جلسة السوق (وأسواق الكريبتو 24/7)
    news.mjs               خلاصات RSS والترجمة وذاكرتها
.github/workflows/
  market-data.yml          كل 10 دقائق أثناء ساعات السوق
  daily-data.yml           مرة يومياً قبل الافتتاح
worker/                    اختياري: Cloudflare Worker للسعر اللحظي الحقيقي
```

---

## 3) مصادر البيانات — الحدود الحقيقية المُختبَرة

هذا الجدول نتيجة فحص مباشر من رينر GitHub Actions، لا نقلاً عن التوثيق:

| المصدر | يعمل لـ | الحدّ | ملاحظة حاسمة |
|---|---|---|---|
| **Finnhub** (مجاني) | أسعار لحظية، أساسيات، ملف الشركة | 60 طلب/دقيقة | **الشموع تعيد 403** — تحتاج اشتراكاً مدفوعاً |
| **Twelve Data** (مجاني) | شموع 15min/1h/1day | 8/دقيقة، **800/يوم** | المصدر الوحيد المجاني الموثوق للشموع |
| **Yahoo Finance** | كل شيء نظرياً | — | **يحظر عناوين رينرات GitHub** — انظر §8 |
| **Stooq** | شموع يومية | — | **يعيد صفحة HTML بحالة 200** من نطاق GitHub |
| **RSS** (Yahoo/MarketWatch/CNBC) | أخبار | — | يعمل بلا مفتاح |

### أسرار مطلوبة في المستودع
`Settings → Secrets and variables → Actions`

| الاسم | الغرض | بدونه |
|---|---|---|
| `FINNHUB_API_KEY` | الأسعار والأساسيات | لا أسعار إطلاقاً |
| `TWELVEDATA_API_KEY` | الشموع والمؤشرات الفنية | يعمل الموقع بلا شارت |

كلاهما تسجيل مجاني ببريد فقط، بلا بطاقة.

### رموز المؤشرات — مصيدة
Finnhub المجاني يرفض `^GSPC` و`^IXIC` و`^DJI` و`^VIX` برسالة
`Market data subscription required for CFD indices`.

**الحل:** استخدم صناديق ETF التي تتبعها، وخذ **نسبة التغيّر فقط**:

| المؤشر | الصندوق البديل |
|---|---|
| S&P 500 | `SPY` |
| Nasdaq | `QQQ` |
| Dow Jones | `DIA` |
| VIX | `VIXY` |

⚠️ لا تعرض سعر الصندوق كأنه مستوى المؤشر (SPY = ‎$769‎ ≠ S&P = ‎6,800‎ نقطة).
اعرض النسبة واترك المستوى شرطة — النسبة هي المطلوبة لمزاج السوق أصلاً.

---

## 4) صِيَغ ملفات البيانات (فرع `data`)

### `summary.json` — صف واحد لكل سهم، تقرأه الشاشة الرئيسية
```jsonc
{
  "updated": 1787942327298,
  "count": 70,
  "rows": [{
    "s": "AAPL", "ar": "أبل", "en": "Apple", "sec": "تقنية",
    "p": 319.64,            // السعر
    "chg": 1.61,            // نسبة التغيّر اليومي
    "ext": {"k":"PRE","p":320.1,"c":0.14},   // ما قبل/بعد الإغلاق، أو null
    "spark": [318.2, 319.0],                 // آخر 30 إغلاقاً للرسم المصغّر
    "score": 84.71,         // النتيجة الفنية −100..+100، أو null
    "atr": 4.82, "rsi": 57.26,
    "tfScore": {"15m": 60.0, "1h": 72.5, "4h": 80.0, "1d": 90.0},
    "mc": 4700000000000,
    // الحجم ونطاق 52 أسبوعاً لا تعطيهما أسعار Finnhub المجانية، فمصدرهما
    // بالترتيب: السعر → حجم آخر شمعة يومية / fundamentals.json
    "vol": 41000000,
    "w52h": 340.1, "w52l": 210.3,
    "stale": false,         // لم تُحدَّث شمعاته في هذا التشغيل
    "src": "twelvedata",    // مصدر الشمعات
    "mkt": "crypto"         // للعملات الرقمية فقط — تخرج به من إحصاء السوق
  }]
}
```

يضمّ الآن 96 صفاً: السبعين المرصودة + 26 عملة رقمية.

### `wide.json` — الطبقة الواسعة (بقية S&P 500)
نفس شكل صفوف `summary.json` بلا `spark` (ثلاثون رقماً لكل صف تضاعف
الحجم بلا فائدة في القائمة)، مع حقل `u` = عمر **الشمعات** لا عمر السعر.
الملف **تراكمي**: كل تشغيل يجدّد حصّته ويدمجها فوق القديم، وبوابة سلامة
ترفض كتابته أقصر مما كان.

### `options/<SYMBOL>.json` · `options.json` — عقود الخيارات
لكل رمز: `spot` و`r` (فائدة) و`q` (توزيعات) و`mode` (`live` أو `last`)
و`hv20` و`ivAtm` و`ivHv`، ثم `exp[]` لكل استحقاق بعقوده السائلة، وكل
عقد يحمل السترايك والقسط ونقطة التعادل واحتمال الانتهاء داخل المال
والجريكس. و`options.json` ملخّص أفضل عقد لكل جانب لكل رمز.

### `backtest.json` · `signals.json` · `archive.json` — الأرشيف
`backtest.json` سجلّ الشروط على خمس سنوات مع **خط أساس** للمقارنة.
`signals.json` سجلّ تراكمي لما ظهر فعلاً في التطبيق، و`archive.json`
تجميعه. الشروط نفسها في `stocks/scans.js` — نسخة واحدة، وإلا قاس
الأرشيف شرطاً غير الذي يراه المستخدم.

### `events.json` — الأحداث القوية
من `federalreserve.gov/json/calendar.json` مباشرة. كل حدث: `at` (بالـUTC
بعد تحويل صحيح للتوقيت الصيفي)، و`ar` بالعربية، و`w` وزنه (3 لقرار
الفائدة، 1 للكتاب البيج).

### `sym/<SYMBOL>.json` — شموع ومؤشرات كل فريم
```jsonc
{
  "s": "AAPL", "v": 2,
  "tf": {
    "1d": {
      "updated": 1787942327298,
      // ⚠️ مضغوطة: [الوقت بالثواني, فتح, أعلى, أدنى, إغلاق, حجم]
      "c": [[1787900000, 318.1, 320.4, 317.8, 319.6, 41000000]]
    },
    "1h": {...}, "15m": {...},
    "4h": {"derived": true, ...}   // مُشتق بتجميع 4 شمعات ساعة
  },
  "an": { "1d": {"score":90,"rsi":57,"atr":4.8,"e20":315,"e50":300,"e200":280,
                 "bbUp":330,"bbLo":305,"bbMid":317,"hist":1.2,"histRising":true} },
  "score": 84.71, "stale": false, "src": "twelvedata",
  "period": {"pre":{...},"regular":{"start":0,"end":0},"post":{...}},
  "errors": ["1h/td: TD: بلا شمعات"]   // للتشخيص فقط
}
```
**الضغط يوفّر ~45٪ من الحجم** (45 ك.ب بدل 95 للسهم). لكنه سبب خلل حقيقي —
انظر §8.

### `market.json` — مزاج السوق
```jsonc
{
  "updated": 1787942327298,
  "status": {"state":"REGULAR","ar":"السوق مفتوح","next":0,"nextAr":"يغلق بعد"},
  "indices": [{"s":"^GSPC","ar":"إس آند بي 500","p":null,"chg":-0.33,"proxy":"SPY"}],
  "sectors": [{"sec":"تقنية","n":20,"avg":1.4}],
  "breadth": {"up":43,"down":27,"flat":0,"total":70},
  "marketScore": 22.4,
  "gainers": [{"s":"AAPL","ar":"أبل","chg":1.61,"p":319.64}],
  "losers": [...]
}
```

### `fundamentals.json` · `ranking.json` · `news.json` · `meta.json`
```jsonc
// fundamentals.json — { updated, count, f: { "AAPL": {...} } }
{ "mc":0,"pe":0,"fpe":0,"pb":0,"eps":0,"divY":0,"divRate":0,"beta":0,
  "w52h":0,"w52l":0,"avgVol":0,"shares":0,"margin":0,"revGrow":0,"roe":0,
  "target":0,"rec":"شراء","recN":42,
  "earnings":{"at":1787900000000,"estimated":true},
  "sector":null,"industry":null,"staff":0,"site":null,"about":"..." }

// ranking.json — { updated, top:["NVDA",...70], mc:{...}, entered:[], exited:[] }
// news.json     — { updated, market:[{title,link,at,source}], sym:{"AAPL":[...]} }
// meta.json     — إحصاءات التشغيل للتشخيص (طلبات، إخفاقات، مصادر)
```

---

## 5) منطق الحسابات

### النتيجة الفنية لفريم واحد: −100 (هبوط قوي) → +100 (صعود قوي)

مجموع موزون لشروط منطقية، مقسوماً على أقصى وزن ممكن:

| الشرط | الوزن |
|---|---|
| السعر فوق EMA200 | 2.5 |
| EMA50 فوق EMA200 | 1.5 |
| السعر فوق EMA20 | 1.0 |
| هيستوغرام MACD موجب | 1.5 |
| الهيستوغرام صاعد عن سابقه | 0.5 |
| RSI فوق 55 (‎+1‎) أو تحت 45 (‎−1‎) وإلا صفر | 1.0 |
| السعر فوق منتصف بولنجر | 0.5 |

```js
score = clamp(-100, 100, (مجموع الموزون / أقصى وزن) × 100)
```

**النتيجة الكلية** = متوسط موزون عبر الفريمات — الأكبر أبطأ وأقل خداعاً:

| الفريم | 15m | 1h | 4h | 1d |
|---|---|---|---|---|
| الوزن | 0.5 | 1 | 1.5 | 2 |

**التسميات:** `≥45` صاعد قوي · `≥15` ميل صاعد · `>−15` عرضي · `>−45` ميل
هابط · وإلا هابط قوي.

### مواصفات المؤشرات
- `EMA(p)`: البذرة = SMA لأول `p` قيمة، ثم `k = 2/(p+1)`
- `RSI(14)`: تنعيم Wilder
- `MACD(12,26,9)` · `BB(20, 2σ)` · `ATR(14)` بتنعيم Wilder
- **فريم 4 ساعات مُشتق** بتجميع كل 4 شمعات ساعة: `o` من الأولى، `c` من
  الأخيرة، `h/l` أقصى/أدنى، `v` مجموع — لأن معظم المصادر لا توفّره

> ⚠️ **مهم:** نفس ملف `indicators.mjs` يعمل في الخادم والمتصفح. لا تكرّر
> المنطق بنسختين وإلا اختلفت الأرقام بين القائمة والشارت.

### حالة السوق
تُشتق من `currentTradingPeriod` لو توفّرت. **وإن لم تتوفّر** (المصدر
محظور) فاحسبها تقريبياً بتوقيت نيويورك:

| الوقت (ET) | الحالة |
|---|---|
| 04:00 – 09:30 | ما قبل الافتتاح |
| 09:30 – 16:00 | مفتوح |
| 16:00 – 20:00 | بعد الإغلاق |
| غير ذلك / سبت وأحد | مغلق |

استخدم `Intl.DateTimeFormat` بـ `timeZone: "America/New_York"` — يتعامل مع
التوقيت الصيفي تلقائياً. (لا يراعي العطلات الرسمية.)

---

## 6) ميزانية الطلبات — أهم قرار هندسي

الحسبة التي تحكم كل شيء:

```
70 رمزاً × 3 فريمات            = 210 طلباً لتغطية كاملة
210 × 8 ثوانٍ (حد 8/دقيقة)     = 28 دقيقة  ← أطول من مهلة المهمة
48 تشغيلاً/يوم × 210           = 10,080 طلباً ← أضعاف حصة الـ800
```

**الحل: تحديث تدريجي.**

- **14 طلباً لكل تشغيل** (~100 ثانية) ← `14 × 48 ≈ 672/يوم` داخل الحصة
- **طلب واحد لكل رمز في التشغيل الواحد** — بلا هذا القيد تبتلع أول ٤ رموز
  الميزانية كاملةً (٣ فريمات لكل رمز) ويبقى ٦٦ رمزاً بلا شمعة لساعات
- **ترتيب الفريمات `1d → 1h → 15m`** — اليومي أساس الشارت والنتيجة الفنية،
  فيمتلئ الأهم أولاً
- **تسلسل الطلبات (لا توازٍ)** — حد 8/دقيقة **عام** لا لكل رمز، والتوازي
  يتجاوزه ويهدر الرصيد على طلبات مرفوضة
- **البيانات السابقة تبقى** كما هي حتى يأتي دورها

النتيجة: تغطية كاملة خلال ~٦ تشغيلات (ساعة واحدة) بدل ١٨.

### أعمار الصلاحية
```js
MAX_AGE = { "15m": 0, "1h": 55*60_000, "1d": 20*3600_000 }
```

---

## 7) مهام GitHub Actions

```yaml
# market-data.yml
on:
  schedule:
    - cron: "*/10 13-21 * * 1-5"      # الجلسة الرسمية (UTC)
    - cron: "*/20 8-12,22-23 * * 1-5" # ما قبل وبعد
  workflow_dispatch:
concurrency: { group: market-data, cancel-in-progress: false }
permissions: { contents: write }
```

**خطوات المهمة بالترتيب:**
1. فحص ذاتي بلا شبكة (`--check`) — يفشل مبكراً لو انكسر المنطق
2. إحضار فرع `data` الحالي إلى `out/` (بناء تراكمي)
3. الجلب والحساب
4. **بوابة سلامة قبل الحفظ** — ترفض الكتابة لو:
   - أقل من 40 صفاً
   - أي صف بسعر غير صالح أو ≤ 0
   - `market.json` بلا حالة سوق
5. الحفظ على فرع `data` كـ **orphan commit** (بلا تاريخ متراكم)
6. ملخص التشغيل

### قواعد ذهبية تعلّمها المشروع بالتجربة
```js
// ✅ لا تكتب فوق بيانات سليمة ببيانات فاشلة
if (!rows.length) throw new Error("لم ينجح أي رمز — لن نكتب فوق البيانات السليمة");
```
```yaml
# ✅ تحقق من وجود كل ملف قبل قراءته في خطوة الملخص،
#    وإلا انهارت بخطأ يخفي السبب الحقيقي
if [ -f out/meta.json ] && [ -f out/summary.json ]; then
```
> ⚠️ **الجدولة تقرأ من الفرع الافتراضي فقط.** التعديل على فرع جانبي لا
> يغيّر شيئاً في التشغيل التلقائي حتى يُدمج.

---

## 8) العقبات الحقيقية وحلولها

> هذا القسم أثمن ما في الوثيقة. كل بند هنا كلّف تشخيصاً فعلياً.

### ⛔ Yahoo Finance يحظر رينرات GitHub — والسبب ليس ما يبدو
**العَرَض:** `HTTP 429` على كل طلب، خلال ثانيتين من بدء المهمة، على عناوين
IP مختلفة كل مرة. ٢١٤ إخفاقاً و**صفر نجاح**.

**ثلاث فرضيات جُرّبت بالترتيب:**

1. *«ازدحام عابر، التراجع يحلّه»* → **خطأ.** التراجع الطويل جعل المهمة
   تعلّق ٢٠ دقيقة وتُلغى بلا بيانات، بينما الفشل السريع كان سيُبقي آخر
   بيانات سليمة. **التراجع الطويل يضرّ حين يكون الحظر ممتداً لا عابراً.**

2. *«ترويسات Referer/Origin تقلّل الرفض»* → **عكس الصحيح.** إرسال `Origin`
   من الخادم يجعل الطلب يبدو تجاوزاً لـ CORS فيُرفض. **لا ترسل `Origin`
   من طرف الخادم إطلاقاً.**

3. *«بصمة TLS»* → صحيحة جزئياً. `curl` نجح بـ 200 في نفس اللحظة التي كان
   `fetch` فيها يأخذ 429. لكن بعد التحويل إلى `curl` عاد الحظر — أي أن
   **نجاح curl كان حظ عنوان IP، والحظر على مستوى النطاق.**

**الخلاصة العملية:** لا تبنِ على Yahoo من بنية تحتية سحابية مشتركة. أبقِه
محاولة ثانية مجانية فقط.

### 🔌 قاطع دورة عند الحظر الممتد
```js
const BREAKER_LIMIT = 25;   // بعد 25 رفضاً متتالياً، توقّف لبقية التشغيل
```
خفّض الطلبات من ٤٩٨ إلى ٣٠ ووفّر ٣ دقائق لكل تشغيل.

> ⚠️ **مصيدة:** Stooq يعيد صفحة حظر بحالة **200** لا 429. لو عددتها نجاحاً
> فستصفّر عدّاد القاطع بعد كل رمز ولن ينفتح أبداً. **اجعل العدّاد للمصدر
> المقصود وحده.**

### 💥 الضغط الذي ينهار عند إعادة الاستخدام
الشمعات تُكتب مضغوطة كمصفوفات `[t,o,h,l,c,v]`، لكن قراءتها في التشغيل
التالي كانت تُمرَّر كما هي إلى حساب يتوقّع كائنات `{t,o,h,l,c,v}` →
`Cannot read properties of undefined (reading 'toFixed')`.

الخلل **كامن منذ اليوم الأول** ولم يظهر إلا بعد أن نجح أول جلب فعلاً —
لأن كل التشغيلات قبله كانت تفشل قبل الوصول إليه.

```js
const unpackCandles = (c) => (Array.isArray(c) && Array.isArray(c[0]))
  ? c.map(a => ({ t: a[0]*1000, o:a[1], h:a[2], l:a[3], c:a[4], v:a[5] }))
  : c;
// طبّقها فور قراءة الملف السابق، فما بعدها يتعامل مع شكل واحد
```
> **درس عام:** أي ضغط عند الكتابة يحتاج فكّاً عند القراءة، واختبار رحلة
> ذهاب وعودة يمسك هذا قبل النشر:
> ```js
> eq(unpack(pack(c)), c);
> if (pack(c)[0].c !== undefined) throw new Error("المضغوط يجب ألا يحمل c");
> if (typeof unpack(pack(c))[0].c.toFixed !== "function") throw new Error("...");
> ```

### 🔇 لا تبتلع الأخطاء بصمت
`catch {}` داخل حلقة على ٧٠ رمزاً يحوّل «المفتاح غير مضبوط» إلى «لا بيانات»
بلا سبب ظاهر. اطبع **أول خطأ فعلي** وعدد الإخفاقات.

### ➖ اعرض شرطة، لا تلفّق رقماً
عند غياب أي حقل اعرض `—`. لا تستبدله بصفر ولا بآخر قيمة معروفة بلا إشارة.
الشمعة الناقصة تُتخطّى، لا تُملأ.

---

## 9) الواجهة (`stocks/index.html`)

ملف واحد، بلا أي مكتبة خارجية، ~٢٤٠٠ سطر، مقسّم إلى أقسام معلّمة.

### خمسة تبويبات
| التبويب | المحتوى |
|---|---|
| ⚡ الآن | مزاج السوق، **الأحداث القوية القادمة**، المؤشرات، الاتساع، القطاعات، الرابحون/الخاسرون |
| 📋 الأسهم | القائمة مع بحث يعمّ **510 رموز** ومبدّل سوق (مرصودة / كل السوق / كريبتو) |
| 🎯 الفرص | ماسح بثمانية شروط، **سجلّ كل شرط تاريخياً**، غلاء العقود عبر السوق، رزنامة الأرباح |
| ⚖️ تحليل | الأقوى أساسياً، متوسطات القطاعات، مقارنة سهمين |
| 📰 الأخبار | أخبار السوق وأخبار كل سهم |

حاسبة المخاطرة والتنبيهات داخل شاشة السهم لا في تبويب مستقل.

### شاشة السهم
شارت شموع على `<canvas>` مع EMA 20/50/200 · أربعة فريمات (تُعطَّل أزرار
الفريمات غير المتاحة للطبقة الواسعة) · دعم ومقاومة · قراءات
RSI/MACD/بولنجر/ATR · بطاقة الشركة (مكرر الربحية، القيمة السوقية، EPS،
التوزيعات، بيتا، نطاق ٥٢ أسبوعاً، عدّ تنازلي للأرباح، **توزيع آراء
المحللين، صفقات المطّلعين**) · **بطاقة عقود الخيارات**.

### حاسبة المخاطرة
```
حجم الصفقة = (رأس المال × نسبة المخاطرة) ÷ (سعر الدخول − وقف الخسارة)
```
مع **تحذير إذا كان الوقف أضيق من ATR** — الوقف داخل نطاق التذبذب الطبيعي
يُضرب عشوائياً.

### قواعد صمود إلزامية
```js
// كل الحقول اختيارية — الواجهة تعمل حتى لو وصلت أسعار بلا شموع
const money = (n) => !isFinite(n) ? "—" : "$" + fmt(n, priceDigits(n));
const k = rec.tf[tf] && rec.tf[tf].c;   // تحقّق قبل الرسم دائماً
if (!k || !k.length) return;
```
- **الملفات التي تُملأ محلياً فقط** (العقود، الطبقة الواسعة، الأرشيف،
  الأحداث) تُخفي بطاقاتها بصمت عند غيابها — لأن غيابها هو الحالة
  الطبيعية على أي تشغيل سحابي، لا خطأ يستحق رسالة.
- **`priceDigits` تتبع حجم الرقم**: خانتان لسهم بـ$320 وثماني خانات
  لعملة بـ$0.0000054، وإلا عُرض السعر السليم "$0.00".
- تخزين آخر نسخة ناجحة في `localStorage` لعرض فوري عند إعادة الفتح
- الملفات الثانوية (أخبار/أساسيات) لا توقف العرض إن فشلت
- تخفيف التحديث حين يكون السوق مغلقاً (البيانات لا تتغيّر)

### `config.js` — نقطة التعديل الوحيدة
```js
window.CFG = {
  DATA_BASE: "https://raw.githubusercontent.com/<user>/<repo>/data",
  LIVE_ENDPOINT: "",        // رابط Cloudflare Worker، أو فارغ للوضع المجاني
  LIVE_POLL_SECONDS: 10,
  FILE_POLL_SECONDS: 120
};
```

---

## 10) ترقية اختيارية: سعر لحظي حقيقي

الوضع الافتراضي يعطي تأخيراً ~١٠ دقائق (دورة المهمة). للسعر اللحظي:
Cloudflare Worker يحمل المفتاح بعيداً عن المتصفح ويخزّن ٥ ثوانٍ — فيبقى
استهلاك الباقة ثابتاً مهما زاد الزوار (١٠٠ زائر = طلب واحد للمزوّد).

```
متغيّرات الـ Worker:  API_KEY · PROVIDER (fmp|twelvedata) · ALLOW (نطاقك)
```
يُفعَّل بوضع رابطه في `config.js`، ويرجع التطبيق تلقائياً للملفات عند أي خطأ.

---

## 11) خطة البناء من الصفر

1. **`symbols.json`** — 90 رمزاً بأسماء عربية وقطاعات، `top: 70`
2. **`indicators.mjs`** — المؤشرات + `analyze` + `overallScore` + `aggregate`،
   وحدة واحدة مشتركة بين الخادم والمتصفح
3. **عملاء المصادر** — كل واحد يعيد **نفس شكل الشمعة**
   `{t,o,h,l,c,v}` فلا يتغيّر ما بعده عند تبديل المصدر
4. **`fetch-market.mjs`** — بميزانية §6 وبوابة السلامة §7
5. **المهمة والفرع** — `data` كـ orphan، والصفحة تقرأ عبر `raw.githubusercontent`
6. **الواجهة** — ابنِها على أن **كل حقل قد يكون `null`**
7. **الأسرار ثم تشغيل يدوي** — راقب `meta.json` لا الشاشة فقط

### فحوص ذاتية لا تُستغنى عنها (`--check`، بلا شبكة)
```
✓ symbols.json صالح والعدد صحيح
✓ stale() يحترم أعمار الفريمات
✓ marketStatus يميّز الجلسات الأربع
✓ approxMarketStatus يميّز الجلسات بتوقيت نيويورك
✓ packCandles/unpackCandles رحلة ذهاب وعودة
✓ aggregate يبني 4h صحيحة من 1h
✓ analyze يعطي إشارات صحيحة (صعود/هبوط/سلسلة قصيرة)
✓ overallScore يزن الفريمات الكبيرة أكثر
✓ num() لا يختلق أرقاماً
✓ rp يحفظ أسعار الأصول الرخيصة ولا يمحوها
✓ Black–Scholes يطابق المثال المرجعي (كول 10.4506 · بوت 5.5735)
✓ تكافؤ الكول والبوت محفوظ
✓ impliedVol يعكس Black–Scholes بدقّة
✓ liquid يرفض العقود الميتة (وضعا السوق المفتوح والمغلق)
✓ etToUTC يحترم التوقيت الصيفي في الاتجاهين
✓ trimArtifacts يقطع سلسلة ما قبل الإدراج
✓ بوابة السجلّ ترفض التقلّص وتسمح بالتشذيب المقصود
```
تسعة سكربتات، ٩٥ فحصاً، كلها بلا شبكة وتمرّ في ثانية.
تُشغَّل كأول خطوة في المهمة، فتفشل خلال ثانية بدل اكتشاف الخلل بعد ٣ دقائق
من الطلبات.

**اختبر الرياضيات بقيم مرجعية معروفة، لا بذاتها.** الخطأ في Black–Scholes
صامت: رقم معقول وخاطئ لا يُميَّز بالنظر، ولا شيء في الواجهة يكشفه.

---

## 12) قائمة تحقّق قبل الإطلاق

- [ ] `.nojekyll` موجود في الجذر
- [ ] الأسرار مضبوطة (`FINNHUB_API_KEY`, `TWELVEDATA_API_KEY`)
- [ ] `DATA_BASE` في `config.js` يشير للمستودع الصحيح
- [ ] المهام مدموجة في **الفرع الافتراضي** (وإلا لن تعمل الجدولة)
- [ ] تشغيل يدوي ناجح، و`meta.json` يُظهر مصادر تعمل
- [ ] بوابة السلامة تمنع الكتابة عند الفشل التام
- [ ] الواجهة تعرض `—` بدل انهيار عند نقص أي حقل
- [ ] لا مفتاح API في أي ملف يصل المتصفح

---

## ملحق: نموذج عميل مصدر بيانات

```js
const BASE = "https://api.example.com";
const TOKEN = process.env.EXAMPLE_API_KEY || "";
export const hasSource = () => Boolean(TOKEN);

export async function fetchCandles(symbol, tf, { outputsize = 260 } = {}) {
  if (!TOKEN) throw new Error("المفتاح غير مضبوط");   // خطأ صريح لا صمت
  const u = new URL(`${BASE}/time_series`);
  u.searchParams.set("symbol", symbol.replace("-", "."));  // BRK-B → BRK.B
  u.searchParams.set("interval", { "15m":"15min", "1h":"1h", "1d":"1day" }[tf]);
  u.searchParams.set("outputsize", String(outputsize));
  u.searchParams.set("order", "ASC");
  u.searchParams.set("apikey", TOKEN);

  const r = await fetch(u);
  if (!r.ok) throw new Error(`HTTP ${r.status}`);
  const j = await r.json();
  // ⚠️ كثير من المزوّدين يعيدون الخطأ بحالة 200 وجسم فيه status:"error"
  if (j?.status === "error") throw new Error(j.message);

  const candles = [];
  for (const v of j.values ?? []) {
    const o = +v.open, h = +v.high, l = +v.low, c = +v.close;
    if ([o,h,l,c].some(x => !Number.isFinite(x))) continue;  // تخطَّ، لا تلفّق
    candles.push({ t: Date.parse(v.datetime + "Z"), o, h, l, c, v: +v.volume || 0 });
  }
  if (!candles.length) throw new Error("بلا شمعات صالحة");
  return { candles: candles.sort((a,b) => a.t - b.t) };
}
```
